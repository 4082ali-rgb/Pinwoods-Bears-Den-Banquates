pdfplumber>=0.10        # text extraction, and page rendering for OCR - no poppler needed
pytest>=7               # tests only

# optional - only needed for scanned/raster PDFs with no text layer. pdfplumber (above)
# renders the pages, so the only extra install is the Tesseract OCR engine itself
# (see README) - no poppler required for OCR.
pytesseract>=0.3        # OCR
